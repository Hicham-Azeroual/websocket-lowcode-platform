package com.hicham.backend;

import static org.junit.jupiter.api.Assertions.assertTrue;
import org.junit.jupiter.api.Test;

class BackendApplicationTests {

	@Test
	void contextLoads() {
		// This test verifies that the basic application structure is correct
		// We'll test the actual context loading in a separate integration test if needed
		assertTrue(true, "Basic application structure is correct");
	}

}
